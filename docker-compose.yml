services:
  app:
    build: .
    image: news-title-virality:latest
    container_name: news-title-virality
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - PYTHONPATH=/app
      - NLTK_DATA=/usr/local/share/nltk_data
    command: bash

  jupyter:
    build: .
    image: news-title-virality:latest
    container_name: news-title-virality-jupyter
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - PYTHONPATH=/app
      - NLTK_DATA=/usr/local/share/nltk_data
    ports:
      - "8888:8888"
    command: >
      jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root
      --NotebookApp.token='' --NotebookApp.password=''

  mlflow:
    build: .
    image: news-title-virality:latest
    container_name: news-title-mlflow
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - PYTHONPATH=/app
    ports:
      - "5000:5000"
    command: >
      mlflow ui --backend-store-uri file:///app/mlruns --host 0.0.0.0 --port 5000

  api:
    build: .
    image: news-title-virality:latest
    container_name: news-title-api
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - PYTHONPATH=/app
      - NLTK_DATA=/usr/local/share/nltk_data
    ports:
      - "8000:8000"
    command: >
      uvicorn src.api.app:app --host 0.0.0.0 --port 8000

  streamlit:
    build: .
    image: news-title-virality:latest
    container_name: news-title-streamlit
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - PYTHONPATH=/app
      - NLTK_DATA=/usr/local/share/nltk_data
    ports:
      - "8501:8501"
    command: >
      streamlit run src/deploy/streamlit_app.py --server.port 8501 --server.address 0.0.0.0
